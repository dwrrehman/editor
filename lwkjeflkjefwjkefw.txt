for (int c = 0; c < lcc; c++) {
				char k = lines[lcl].data[c];
				if (length >= wrap_width) length = 0;
				if (k == '\t') { 
					do {
						if (length >= wrap_width) length = 0;
						length++; 
					} while (length % tab_width); 
				} else if (visual(k)) length++;
			}

			while (vcc <) {
				
			}